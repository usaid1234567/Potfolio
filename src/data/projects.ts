import type { Project } from "@/types";

/** No projects have been added yet. Add real, shipped work here as it becomes available —
 * never populate this with placeholder or invented projects. */
export const PROJECTS: Project[] = [];
